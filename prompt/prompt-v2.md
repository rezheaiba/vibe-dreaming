## bug
1、用户登录后，我重启后台服务，但是页面还处在登录状态（登录态持久化存储），此时进行梦境提交等操作会报错
2、打卡界面有bug，页面太宽，重新修改设计

## 修改项目

### 管理员相关
1、在用户表中新增列，该列表明该用户是管理员还是普通用户，登录时系统判断属性，如果是用户则登陆用户页面，如果是管理员则进入管理员页面
2、管理员不应该显示在用户界面主页下方，只有管理员登录时，主页下方才会有管理员的标签按钮
3、管理员进行对记录或用户的增删改查时，需要同步到对应的数据库表
4、增加审核机制和内容检测，如果充斥暴力色情内容，则无法发布，仅可以私有化存在本地，不能发布

## 用户相关
1、`我的梦境`页面中，搜索梦境关键词需要有搜索的点击按钮
2、当日新增梦境记录后，自动打卡，是否需要新增打卡表？或是怎样处理
